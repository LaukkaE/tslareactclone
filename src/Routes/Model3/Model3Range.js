import React from 'react';
import background from '../../pics/model3/model3range.jpg';

const Model3Range = () => {
    return (
        <div
            className="model3range background"
            style={{ backgroundImage: `url(${background})` }}
        >
            Model3Main
        </div>
    );
};

export default Model3Range;
